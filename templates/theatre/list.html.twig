<table border="1">
	<tr>
		<th>id</th>
		<th>title</th>
		<th>Genre</th>
		<th>duration</th>
		<th>action</th>
	</tr>
	{%for list in list%}
		<tr>
			<td>{{list.id}}</td>
			{# <td>{{list.title|upper}}</td> #}
			<td>{{ list.title|slice(1,2)}}</td>


			<td>{{list.genre}}</td>
			<td>{{list.duration}}</td>
			<td>
				<a href="{{ path('Add_Show') }}">Add Show</a>

			</td>
			<td>
				<a href="{{ path('th_update',{ 'id': list.id}) }}">update</a>
<a href="{{ path('app_delete', { 'id': list.id }) }}">Supprimer</a>



			</td>


		</tr>
		{%endfor%}
	</table>
